{
  "name": "mealie-recipe-panel",
  "version": "0.1.0",
  "description": "Touch-friendly Mealie recipe browser for Home Assistant (sidebar panel + launcher card)",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "node node_modules/typescript/bin/tsc --noEmit",
    "build": "npm run typecheck && node esbuild.config.mjs"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "esbuild-wasm": "0.28.1",
    "typescript": "^5.7.2"
  }
}
