{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Mealie",
        "description": "Enter your Mealie instance URL and an API token (Mealie → Settings → API Tokens).",
        "data": {
          "url": "Mealie URL",
          "mealie_token": "API Token"
        }
      }
    },
    "error": {
      "cannot_connect": "Could not connect to Mealie at that URL.",
      "invalid_auth": "Mealie rejected that API token."
    },
    "abort": {
      "already_configured": "This Mealie instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "AI Recipe Finder",
        "description": "Optional: pick which AI Task entities power the AI recipe finder. Leave the text entity unset to hide that feature.",
        "data": {
          "ai_text_entity": "AI Task entity for recipe text",
          "ai_image_entity": "AI Task entity for recipe images (optional)"
        }
      }
    }
  }
}
